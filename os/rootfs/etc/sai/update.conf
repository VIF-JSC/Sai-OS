# sai-update configuration.
# UPDATE_ENABLED=0 disables automatic updates on this machine.
UPDATE_ENABLED=1
# UPDATE_APT=0 runs migrations only, without upgrading packages.
UPDATE_APT=1
# UPDATE_SNAPSHOT=0 skips the Timeshift snapshot before updating.
UPDATE_SNAPSHOT=1
